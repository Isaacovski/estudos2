/*
Escreva uma função que recebe um array não vazio de inteiros distintos e um inteiro 
representando uma soma alvo. Se quaisquer dois números no array de entrada somam a
soma alvo, a função deve retornar-los no array de saída, em qualquer ordem. Se nenhum
par de números no array de entrada somarem a soma alvo, a função deve retornar um array
vazio.

Note que a soma alvo deve ser obtida somando dois inteiros diferentes no array, você
não pode somar um inteiro com ele mesmo.
você pode assumir que existirá no maxímo um par de números somando a soma alvo.
Exemplo de entrada.

array = [3, 5, -4, 8, 11, 1, -1, 6]
targetSum = 10
Exemplo de saida
[-1, 11] // pode ser na ordem invertida
*/

const array = array = [3, 5, -4, 8, 11, 1, -1, 6]
const targetSum = 10

function somaAlvo(arr, soma) {
arr.forEach(element => {
  console.log(elem)
  });
}

somaAlvo(array, targetSum)